/**
 * @deprecated Use migrate-production-to-newlistifysdb.ts
 * npm run migrate:prod
 */
import { logger } from "../utils/logger.js";

logger.info(
  "Stub only — run: npm run migrate:prod (dry-run) or npm run migrate:prod:write",
);
process.exit(0);
